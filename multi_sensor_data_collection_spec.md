# 6V5L 多传感器融合数据采集规范

## 1. 适用范围

本文档适用于当前工程的多传感器融合数据采集、落盘、标定和交付检查。当前目录 `001/` 表示一个场景编号，工程采用 6V5L 方案：6 路相机与 5 路定向激光雷达。

当前样例目录中仅落盘了部分数据；空文件夹应视为同名传感器在完整采集中也需要落盘相同类型的数据和元数据。

## 2. 当前工程结构约定

```text
001/
├── camera/
│   ├── front_camera/
│   ├── front_left_camera/
│   ├── front_right_camera/
│   ├── back_camera/
│   ├── back_left_camera/
│   └── back_right_camera/
├── lidar/
│   ├── front_lidar/
│   ├── front_left_lidar/
│   ├── front_right_lidar/
│   ├── back_left_lidar/
│   └── back_right_lidar/
└── meta/
    ├── gps.json
    └── timestamps.json
```

传感器目录命名必须稳定，不允许在不同场景之间改变含义。建议所有场景均保持完整目录结构，即使某一传感器短时缺帧，也不得删除对应传感器目录。

## 3. 场景与文件命名

1. 每个场景使用三位或固定宽度编号作为目录名，例如 `001`、`002`。
2. 传感器数据文件名必须使用采集时间戳命名，不含额外前后缀。
3. 时间戳单位统一为微秒，使用整数表示，例如 `1557539924499810`。
4. 相机图像文件命名格式：`<timestamp>.jpg`，例如 `1557539924499923.jpg`。
5. 激光雷达点云文件命名格式：`<timestamp>.pcd`，例如 `1557539924499810.pcd`。
6. 同一传感器目录内文件必须按时间戳单调递增；同一场景中不得出现重复时间戳文件名。

## 4. 采集帧率要求

当前样例中相机与激光雷达相邻帧间隔约为 100 ms，对应约 10 Hz。完整采集建议以 10 Hz 作为融合主频；如果项目需要更高或更低频率，必须在场景元数据中明确记录每类传感器的实际采集频率。

采集要求如下：

1. 6 路相机建议统一采集频率，默认 10 Hz。
2. 5 路激光雷达建议统一采集频率，默认 10 Hz。
3. GPS/IMU/里程计等定位信息频率可高于主频，但必须能插值或匹配到每个融合帧。
4. 不允许用不稳定的跳帧数据作为合格采集结果。若存在缺帧，必须在 `meta/timestamps.json` 中记录并在质检报告中说明。
5. 每个融合帧应包含 6 路相机、5 路激光雷达以及对应定位信息。若工程允许部分传感器缺失，必须显式标记缺失项，不得用空文件伪造数据。

## 5. 时间同步要求

传感器时间同步可以采用硬同步或软同步，但交付数据必须满足统一的时间差约束。

1. 任意两个传感器在同一融合帧内的采集时间差必须控制在 20 ms 以内。
2. 时间差约束适用于 camera-camera、lidar-lidar、camera-lidar、sensor-GPS/IMU 等组合。
3. 硬同步方案应记录触发源、触发频率、触发延迟和每个传感器的曝光/扫描时间戳定义。
4. 软同步方案应记录时间戳来源、主时钟、插值方式、匹配策略和最大匹配阈值。
5. 软同步不得仅依赖文件写入时间；必须使用传感器采集时刻或驱动层硬件/系统时间戳。
6. 每个融合帧建议选择 `front_lidar` 或统一主时钟时间戳作为主时间戳，其余传感器向该时间戳匹配。

## 6. 相机数据要求

### 6.1 相机列表

完整 6V5L 方案应包含以下 6 路相机：

- `front_camera`
- `front_left_camera`
- `front_right_camera`
- `back_camera`
- `back_left_camera`
- `back_right_camera`

### 6.2 图像格式

1. 图像必须是已经去除畸变后的图像。
2. 去畸变应在落盘前完成，交付目录中的图像不应再包含原始镜头畸变。
3. 图像建议使用 JPEG 或 PNG。当前样例为 JPEG，分辨率为 1920x1080，8-bit sRGB。
4. 若使用 JPEG，应控制压缩质量，避免明显压缩伪影影响标定和感知任务。建议质量不低于 90；若使用样例类似质量 75，必须确认满足下游精度要求。
5. 同一相机在同一场景内分辨率、色彩空间、位深和编码格式必须保持一致。
6. 图像文件内容不得旋转、镜像或裁剪，除非该操作已纳入内参和标定结果。

### 6.3 相机内参 `intrinsics.json`

每路相机目录下必须包含 `intrinsics.json`。当前样例字段如下：

```json
{
  "fx": 933.4667,
  "fy": 934.6754,
  "cx": 896.4692,
  "cy": 507.3557
}
```

字段要求：

1. `fx`、`fy` 为去畸变后图像使用的焦距，单位为像素。
2. `cx`、`cy` 为去畸变后图像使用的主点坐标，单位为像素。
3. 因交付图像已经去畸变，默认不再使用畸变参数参与投影。
4. 若必须记录原始畸变参数，应另存为扩展字段或独立文件，并明确标注其对应原始图像，不得与去畸变后内参混用。
5. 内参必须与落盘图像分辨率严格匹配。

### 6.4 相机姿态 `poses.json`

每路相机目录下必须包含 `poses.json`，数组长度应与该相机图像数量一致，并按时间戳升序排列。

```json
[
  {
    "timestamp": 1557539924499923,
    "position": {"x": -0.08826033786496988, "y": -0.2570137236715922, "z": 1.6686910930852348},
    "orientation": {"qw": -0.2796245614154197, "qx": 0.24665037288396235, "qy": 0.6617038562774442, "qz": -0.6504780586591647}
  }
]
```

姿态语义必须按以下规则执行：

1. 第一帧姿态表示 `camera2ego`，即第一帧相机坐标系到 ego 坐标系的变换。
2. 从第二帧开始，每帧姿态均表示相对第一帧 `camera2ego` 的姿态变化。
3. 如需恢复第 `i` 帧的相机到 ego 变换，应使用第一帧 `camera2ego` 与第 `i` 帧相对姿态按约定顺序组合。
4. `position` 使用米作为单位。
5. `orientation` 使用四元数，字段顺序固定为 `qw, qx, qy, qz`。
6. 四元数必须归一化。
7. `timestamp` 必须与对应图像文件名一致。

## 7. 激光雷达数据要求

### 7.1 激光雷达列表

完整 6V5L 方案应包含以下 5 路定向激光雷达：

- `front_lidar`
- `front_left_lidar`
- `front_right_lidar`
- `back_left_lidar`
- `back_right_lidar`

其中 `front_lidar` 是主视激光雷达，也是多雷达拼接和默认 ego 坐标系的重要基准。

### 7.2 点云格式

每帧点云必须使用 PCD v0.7 格式，推荐以二进制方式保存。当前样例已采用以下格式：

```text
# .PCD v0.7 - Point Cloud Data file format
VERSION 0.7
FIELDS x y z intensity
SIZE 4 4 4 4
TYPE F F F F
COUNT 1 1 1 1
WIDTH <point_count>
HEIGHT 1
VIEWPOINT 0 0 0 1 0 0 0
POINTS <point_count>
DATA binary
```

字段要求：

1. 必须包含 `x y z intensity` 四个字段。
2. `x`、`y`、`z` 单位为米，类型为 float32。
3. `intensity` 类型为 float32，取值范围建议归一化到 `[0.0, 1.0]`。
4. 点云必须保存为 `DATA binary` 或 `DATA binary_compressed`，不得交付 ASCII PCD。
5. 若原始点云是 ASCII PCD，交付前必须转换为二进制 PCD。
6. 同一数据集内建议统一使用 `DATA binary`；如使用 `binary_compressed`，必须确保下游解析工具支持。
7. `WIDTH` 与 `POINTS` 必须等于实际点数，`HEIGHT` 对非结构化点云固定为 1。

### 7.3 激光雷达姿态 `poses.json`

每路激光雷达目录下必须包含 `poses.json`，数组长度应与该雷达 PCD 文件数量一致，并按时间戳升序排列。

姿态语义必须按以下规则执行：

1. 第一帧姿态表示 `lidar2ego`，即第一帧激光雷达坐标系到 ego 坐标系的变换。
2. 从第二帧开始，每帧姿态均表示相对第一帧 `lidar2ego` 的姿态变化。
3. 如需恢复第 `i` 帧的激光雷达到 ego 变换，应使用第一帧 `lidar2ego` 与第 `i` 帧相对姿态按约定顺序组合。
4. `position` 使用米作为单位。
5. `orientation` 使用四元数，字段顺序固定为 `qw, qx, qy, qz`。
6. 四元数必须归一化。
7. `timestamp` 必须与对应 PCD 文件名一致。

## 8. 多传感器标定要求

### 8.1 坐标系约定

1. 所有传感器外参最终必须能统一到 ego 坐标系。
2. 如具备车辆 ego 坐标系标定条件，应以车辆定义的 ego 坐标系为准。
3. 如果没有标定 ego 的条件，则以主视激光雷达 `front_lidar` 所在坐标系作为 ego 坐标系。
4. 坐标系定义必须在标定报告中写明，包括轴向、原点、单位和右手/左手系约定。

### 8.2 多雷达标定与拼接

1. 5 个激光雷达均为定向 lidar，不应假设单雷达具备 360 度完整视场。
2. `front_lidar` 是主视 lidar，多雷达标定时所有 `lidar2lidar` 外参必须向 `front_lidar` 对齐。
3. 先完成 `front_left_lidar`、`front_right_lidar`、`back_left_lidar`、`back_right_lidar` 到 `front_lidar` 的外参标定。
4. 使用 `lidar2front_lidar` 外参将 5 路点云拼接到 `front_lidar` 坐标系。
5. 拼接点云应检查重叠区域的一致性，包括边缘重影、地面连续性、静态目标轮廓和强度分布。
6. 多雷达拼接合格后，再基于拼接后的点云进行所有相机的 camera-lidar 标定。

### 8.3 相机与点云标定

1. 相机标定应基于 5 路 lidar 拼接后的点云，而不是仅使用单个定向 lidar 的局部点云。
2. 每路相机均需标定 `camera2ego` 或等价的 `camera2front_lidar` 外参。
3. 若 ego 使用 `front_lidar` 坐标系，则相机外参应能直接或间接转换到 `front_lidar` 坐标系。
4. 标定使用的图像必须与交付图像一致，均为去畸变后的图像。
5. 标定完成后应进行图像-点云投影检查，重点检查近处目标、远处目标、左右边缘、上下边缘和重叠视场。

## 9. 元数据要求

### 9.1 `meta/gps.json`

`gps.json` 记录场景内定位信息。当前样例格式如下：

```json
[
  {
    "timestamp": 1557539924499810,
    "lat": 37.77466814570412,
    "long": -122.40106707219165,
    "height": 2.938702280237465,
    "xvel": 0.017196079484729808,
    "yvel": 7.733904794845615
  }
]
```

字段要求：

1. `timestamp` 使用微秒时间戳。
2. `lat`、`long` 使用 WGS84 经纬度，单位为度。
3. `height` 使用米作为单位。
4. `xvel`、`yvel` 使用米每秒作为单位，并需说明所在坐标系。
5. 若有航向、姿态、定位质量、RTK 状态或协方差信息，建议扩展记录。

### 9.2 `meta/timestamps.json`

完整采集建议提供 `timestamps.json`，用于描述融合帧与各传感器文件的匹配关系。推荐格式如下：

```json
[
  {
    "frame_id": 0,
    "timestamp": 1557539924499810,
    "camera": {
      "front_camera": 1557539924499900,
      "front_left_camera": 1557539924499800,
      "front_right_camera": 1557539924499850,
      "back_camera": 1557539924499923,
      "back_left_camera": 1557539924518607,
      "back_right_camera": 1557539924499700
    },
    "lidar": {
      "front_lidar": 1557539924499810,
      "front_left_lidar": 1557539924499790,
      "front_right_lidar": 1557539924499820,
      "back_left_lidar": 1557539924499760,
      "back_right_lidar": 1557539924499880
    },
    "gps": 1557539924499810
  }
]
```

要求：

1. `timestamp` 为融合帧主时间戳。
2. 各传感器时间戳必须能对应到实际落盘文件。
3. 同一融合帧内任意两个传感器时间戳差值不得超过 20 ms。
4. 若某传感器缺帧，字段值应为 `null`，并在质检报告中说明原因。
5. `timestamps.json` 应覆盖场景内全部融合帧。

## 10. 数据完整性检查

交付前必须完成以下检查：

1. 目录结构包含 6 个相机目录、5 个 lidar 目录和 `meta` 目录。
2. 每路相机包含图像文件、`intrinsics.json` 和 `poses.json`。
3. 每路 lidar 包含 PCD 文件和 `poses.json`。
4. 图像已去畸变，且内参与去畸变后图像匹配。
5. PCD 文件包含 `x y z intensity`，且不是 ASCII 保存。
6. 每个 `poses.json` 的长度与对应传感器数据帧数一致。
7. pose 中 `timestamp` 与对应数据文件名一致。
8. 每个融合帧中任意两传感器时间差不超过 20 ms。
9. 相邻帧间隔符合设定采集帧率，默认 10 Hz 时应约为 100 ms。
10. 四元数已归一化，无 NaN、Inf 或异常跳变。
11. GPS/定位数据可匹配到融合帧。
12. 标定外参已通过多雷达拼接检查和图像-点云投影检查。

